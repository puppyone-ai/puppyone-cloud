import { PageLoading } from '@/components/loading';

/** Only route content suspends; project chrome belongs to the parent layout. */
export default function ProjectSegmentLoading() { return <PageLoading variant='fill' />; }
