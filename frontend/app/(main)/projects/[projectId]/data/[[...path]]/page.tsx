/** URL entry only. The stable Files layout renders the current route through
 * useWorkspaceLocation, including refreshes and browser history restoration. */
export default function FileRoute() { return null; }
